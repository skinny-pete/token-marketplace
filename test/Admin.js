// it('Set approval', async () => {
//     await marketplace.setSellers([buyer.address], [true]);
//     expect(await marketplace.sellerApprovals(buyer.address)).to.equal(true);
//   });

// only deployer can call function
